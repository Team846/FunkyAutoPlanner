

//scp's single file from the public folder to the deploy folder in the 10.8.46.2 device
export function scpFile(file : string){

}

//scp's entire contents of deploy folder in the public folder to the deploy folder in the 10.8.46.2 device
export function scpFolder(){

}